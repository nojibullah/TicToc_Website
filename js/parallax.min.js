jQuery(document).ready(function($) {   
    $("[data-image-src]").each(function() {
      image_src = $(this).data("image-src");
      $(this).addClass('background-parallax-container'); 
      $(this).append('<div class="background-parallax" style="background-image:url(' + image_src + ')"></div>');
    });
});